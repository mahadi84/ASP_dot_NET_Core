

01. Command to Create DB, Table, Data insert into Table, and Show

02. How to create a project and  install the .NET necessary packages, and WHY are these packages needed?
03. How to check Database Connectivity and show data from the database table?

04. How in real project a database we connect? 
05. Migration, what is it? Without migration, what problems will we face?
06. Development vs Production Version of a Software with packages used?




===========================================================================
MySQL DATABASE(MySQL Workbench): Create DB, TABLE, INSERT and CHECK DATA
===========================================================================

CREATE DATABASE bank_server_db;
use bank_server_db;

CREATE TABLE bank_servers (
    server_id INT PRIMARY KEY AUTO_INCREMENT,
    server_name VARCHAR(100) NOT NULL,
    purpose VARCHAR(255) NOT NULL
);

INSERT INTO bank_servers (server_name, purpose)
VALUES 
('Core Banking Server', 'Handles customer accounts and transactions'),
('ATM Switch Server', 'Processes ATM transactions'),
('Database Server', 'Stores banking data');

SELECT * FROM bank_servers;


============================ .NET Core  =============================


### 01. Create a new Folder->Enter-> and open CMD typing in URL:
dotnet new mvc -n BankServerCrud   # Optional(-f net9.0)
cd MyMvcApp                        # Enter into the project folder




### 02. Install Required NuGet Packages **Entity Framework Core (EF Core)
** OR Direct Folder-CMD-LINE OR
.NET CLI:(View>Terminal>Select:Developer Command Prompt) Select:Developer Command Prompt)

dotnet tool install --global dotnet-ef --version 9.0.0                    # The Steering Wheel-This installs a global command-line tool on my computer.
dotnet add package Microsoft.EntityFrameworkCore.Design --version 9.0.0   # The Engine, development library directly into your specific project file (.csproj).
dotnet add package Pomelo.EntityFrameworkCore.MySql --version 9.0.0       # To connect MySQL


[You Type Command e.g: "dotnet ef migrations add AddUserTable"] 
       │
       ▼
[dotnet-ef Tool] (Reads your command, verifies the "migrations add" syntax, and locates your project)  ## If I miss the tool, your terminal will say "Command not found"
       │
       ▼
[EFCore.Design Package] (Compiles your project, scans your model classes, and writes the C# migration files)      ##If you miss the design package, the tool will crash and say "Your startup project doesn't reference Microsoft.EntityFrameworkCore.Design".




###  03. Create a data Folder and file AppDbContext.cs
Now it should look like this:

BankServerCrud
│
├── Models
│
├── Data
│   └── AppDbContext.cs
.
.
├── Program.cs
└── BankServerCrud.csproj




###  04. Copy and paste the code below and update DB info into: AppDbContext.cs


using Microsoft.EntityFrameworkCore;
namespace BankServerCrud.Data;

public class AppDbContext : DbContext
{
    protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)
    {
        string connectionString =
            "Server=localhost;Port=3306;Database=bank_server_db;User=root;Password=Mahadi@123;";

        optionsBuilder.UseMySql(
            connectionString,
            ServerVersion.AutoDetect(connectionString)
        );
    }
}



#### 05. Delete everything and paste this in: Program.cs

using BankServerCrud.Data;
using Microsoft.EntityFrameworkCore;

Console.WriteLine("====================================");
Console.WriteLine("   Show Data from MySQL Table");
Console.WriteLine("====================================");

try
{
    using var db = new AppDbContext();

    Console.WriteLine("Trying to connect to MySQL...");

    bool isConnected = db.Database.CanConnect();

    if (!isConnected)
    {
        Console.WriteLine("Database connection failed.");
        return;
    }

    Console.WriteLine("Database connected successfully.");
    Console.WriteLine();

    var connection = db.Database.GetDbConnection();

    connection.Open();

    using var command = connection.CreateCommand();

    command.CommandText = @"
        SELECT server_id, server_name, purpose
        FROM bank_servers
        ORDER BY server_id;
    ";

    using var reader = command.ExecuteReader();

    Console.WriteLine("{0,-10} {1,-25} {2,-40}", "ID", "Server Name", "Purpose");
    Console.WriteLine(new string('-', 80));

    bool hasData = false;

    while (reader.Read())
    {
        hasData = true;

        int serverId = reader.GetInt32(0);
        string serverName = reader.GetString(1);
        string purpose = reader.GetString(2);

        Console.WriteLine(
            "{0,-10} {1,-25} {2,-40}",
            serverId,
            serverName,
            purpose
        );
    }

    if (!hasData)
    {
        Console.WriteLine("No data found in bank_servers table.");
    }

    connection.Close();
}
catch (Exception ex)
{
    Console.WriteLine("Error occurred.");
    Console.WriteLine("------------------------------------");
    Console.WriteLine($"Error Message: {ex.Message}");
    Console.WriteLine("------------------------------------");
}













=========================== In Real Apps DB Connection is like this ============================

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
| Part             | Definition                                                                        | One-line example                                                                                                                          |
| ---------------- | --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| appsettings.json | Stores application settings, including the database connection string.            | `"DefaultConnection": "Server=localhost;Database=Emp;User=root;Password=Mahadi@123;"`                                                     |
| Program.cs       | Reads the connection string and registers `AppDbContext` in dependency injection. | `builder.Services.AddDbContext<AppDbContext>(options => options.UseMySql(connectionString, ServerVersion.AutoDetect(connectionString)));` |
| AppDbContext     | Represents the database connection layer between the app and MySQL using EF Core. | `public DbSet<BankServer> BankServers { get; set; }`                                                                                      |
| Controller       | Handles user requests and uses `DbContext` to get, save, update, or delete data.  | `var servers = await _context.BankServers.ToListAsync();`                                                                                 |
| View             | Shows data to the user through Razor `.cshtml` pages.                             | `@foreach (var server in Model) { <td>@server.ServerName</td> }`                                                                                                              |
| MySQL            | Stores the real data inside tables.                                               | `SELECT * FROM bank_servers;`                                                                                                             |
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


Flow:
appsettings.json → Program.cs → AppDbContext → Controller → View(Index.cshtml) → User
                                      ↓
                                    MySQL



===========================================================================
                  Migration Vs Without Migration
===========================================================================

The "Dual-Work" Trap (Manually Syncing Code & DB):
Every time you add, change, or delete a property in your C# models, you have to manually open a database tool (like MySQL Workbench) and write raw SQL commands to match it.

With Migrations: You change C# $\rightarrow$ Run command $\rightarrow$ Database updates automatically.
Without Migrations: You change C# $\rightarrow$ You manually write ALTER TABLE Users ADD Column Phone VARCHAR(20); in MySQL. If you make a typo in the database column name, your app crashes.




================================================= 
   DEVELOPMENT vs PRODUCTION 
================================================

Think of your software development cycle in two distinct phases: 
Construction (Development) and Operation (Production).

---

### Phase 1: Construction (Building the House)

This is where `dotnet-ef` and `EFCore.Design` work. They act as the architect and blueprint machines.

 They only run on your laptop/development machine.
 Their sole job is to look at your C# classes and write out the blueprint files (the `Migrations` folder in your code).
 Once the blueprints are generated, their job is completely finished.

---

### Phase 2: Operation (Living in the House)

When you publish your API or Web App and ship it to a server (like AWS, Azure, or a Linux VPS), you do not ship `dotnet-ef` or `EFCore.Design`. They are left behind.

 The live running application doesn't need to generate blueprints anymore; it just needs to read and write data.
 The only thing running on the server is your App Code and the Pomelo.MySql Provider (the connection pipe to the database).

---

### Summary Checklist

----------------------------------------------------------------------------------------------------------------------
| Package / Tool                           | Needed on your Laptop (Dev)?        | Needed on the Live Server (Prod)? |
| ---------------------------------------- | ----------------------------------- | --------------------------------- |
| `dotnet tool install --global dotnet-ef` | YES (To type commands)              | NO                                |
| `Microsoft.EntityFrameworkCore.Design`   | YES (To generate C# code files)     | NO                                |
| `Pomelo.EntityFrameworkCore.MySql`       | YES (To connect locally)            | YES (To connect live)             |
----------------------------------------------------------------------------------------------------------------------



.
