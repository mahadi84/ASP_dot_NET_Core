




## 🧱 0. Clean Architecture skeleton-

CBS
 └─ src
    ├─ CBS.Domain
    │   └─ Entities
    │       └─ User.cs
    |       └─ Customer.cs
    │
    ├─ CBS.Application
    │   ├─ Interfaces
    │   │   └─ ICustomerService.cs
    │   └─ Services
    │       └─ CustomerService.cs
    │
    ├─ CBS.Infrastructure
    │   ├─ Persistence
    │   │   └─ AppDbContext.cs
    │   └─ Repositories
    │       └─ CustomerRepository.cs
    │
    └─ CBS.Web
        └─ Controllers
        |    └─ CustomerController.cs
        └─ Models
        |    └─ ViewModel.cs
        └─ Views
            └─ User
                └─ User.cshtml

---



## 🏁 1. Solution + Projects তৈরি (dotnet CLI দিয়ে)
👉 Command Prompt বা PowerShell – যেকোনো একটাতে এইগুলো রান করবে।

```powershell
cd D:\AsPdOtNeT\DotNet

mkdir CBS
cd CBS

mkdir src
cd src


## Creates  C# Class Library(create .csproj file) for A PROJECT(a room of a house), 
## serves as a reusable container for your core business logic and entities.

dotnet new classlib -n CBS.Domain
dotnet new classlib -n CBS.Application
dotnet new classlib -n CBS.Infrastructure
dotnet new mvc -n CBS.Web



## Solution file named CBS.sln,a master CONTAINER(Architecture of Whole house)
## to manage and build all individual projects (Domain, Infrastructure, API, etc.) as a single unit.

cd ..   # Go back to the main folder (CBS)
dotnet new sln -n CBS

## এখন সব project-কে solution-এ add করি:
dotnet sln CBS.sln add src\CBS.Domain\CBS.Domain.csproj
dotnet sln CBS.sln add src\CBS.Application\CBS.Application.csproj
dotnet sln CBS.sln add src\CBS.Infrastructure\CBS.Infrastructure.csproj
dotnet sln CBS.sln add src\CBS.Web\CBS.Web.csproj


---

## 🔗 2. Dependency (নির্ভরশীলতা) বা Layer (inner/Onion) Connection তৈরি করে।
          
      Web->Infrastructure->Application->Domain
 
     # 2.১. Application শুধু Domain-কে চিনবে (Entities & Interfaces)
     dotnet add src\CBS.Application reference src\CBS.Domain
     
     # 2.২. Infrastructure, Domain-কে চিনবে (Entities সেভ করার জন্য)  এবং  'Application' কেও চিনবে শুধু ইন্টারফেস ইমপ্লিমেন্ট করার জন্য
     dotnet add src\CBS.Infrastructure reference src\CBS.Domain
     dotnet add src\CBS.Infrastructure reference src\CBS.Application
     
     # 2.৩. Web API সব লেয়ারকে চিনবে (Dependency Injection বা আঠা দিয়ে জোড়া লাগানোর জন্য)
     dotnet add src\CBS.Web reference src\CBS.Domain
     dotnet add src\CBS.Web reference src\CBS.Application
     dotnet add src\CBS.Web reference src\CBS.Infrastructure

          ## 2.4 Dependency-Reference  চেক করা: 
           cd src/CBS.Infrastructure  # প্রথমে ওই প্রোজেক্টের ফোল্ডারে ঢুকুন
           dotnet list reference      # রেফারেন্স লিস্ট দেখুন


## 📁 3. NuGet Packages

dotnet add src\CBS.Infrastructure package MySqlConnector                                    # এটি মূলত ডাটাবেসের সাথে কথা বলার ড্রাইভার (যেমন: MySqlConnection তৈরি করা)।
dotnet add src\CBS.Infrastructure package MySqlConnector.DependencyInjection                # এটি ছাড়া কম্পাইলার services অবজেক্টের টাইপ চিনতে পারবে না।
dotnet add src/CBS.Infrastructure package Microsoft.Extensions.Configuration.Abstractions   # এটি ছাড়া কম্পাইলার services-Interface  টাইপ চিনতে পারবে না।

dotnet add src\CBS.Infrastructure package Dapper              # For Dapper

dotnet add src\CBS.Infrastructure package dbup-mysql          # To Create Migration File for MySQL database

dotnet add src\CBS.Infrastructure package BCrypt.Net-Next     # ইউজারদের পাসওয়ার্ড Hash করার জন্য । যাতে ডাটাবেস হ্যাক হলেও কেউ আসল পাসওয়ার্ড না পায়।
dotnet add src\CBS.Infrastructure package QuestPDF            # এই লাইব্রেরিটি অ্যাপ্লিকেশন থেকে PDF রিপোর্ট বা ইনভয়েস তৈরি করার জন্য ব্যবহৃত হয়।

-- for using ef--
dotnet add src\CBS.Infrastructure package Pomelo.EntityFrameworkCore.MySql  --version 9.0.0      # MySQL ডাটাবেসের সাথে কানেক্ট করার জন্য এটি একটি জনপ্রিয় ডাটাবেস প্রোভাইডার।
dotnet add src\CBS.Infrastructure package Microsoft.EntityFrameworkCore.Tools --version 9.0.0    # মাইগ্রেশন এবং অন্যান্য কমান্ড চালানোর জন্য।
dotnet add src\CBS.Infrastructure package Microsoft.EntityFrameworkCore.Design --version 9.0.0   # এই প্যাকেজটি ডাটাবেস Migrations এবং স্ক্যাফোল্ডিং (Scaffolding) করার জন্য প্রয়োজনীয় টুলস প্রদান করে।
dotnet add src\CBS.Web package Microsoft.EntityFrameworkCore.Design --version 9.0.0              # মাইগ্রেশন ফাইলগুলো জেনারেট করার জন্য ব্যবহৃত হয়।



### 4. appsettings.json:

{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=MyDb;User=root;Password=your_password;"
  }
}


### 5. MySQL connect and Migration File create(Program.cs)
** This is for "Clean Architecture"
  as Dependancy Installed in Infrastructre layer and Injection in  Web Route(Program.cs)

 Create a Folder,File & text into .cprog file in Infrastructure layer:
 --------------------------------------------------------------------
      Scripts folder>001_Crate_____Table.sql (and put sql with table name and info)
      Add in CBS.Infrastructure.cprog:
         <ItemGroup>
           <EmbeddedResource Include="Scripts\*.sql" />   ## will find Scripts folder and the .sql file in this layer to migrate
         </ItemGroup>



Create File_01:
---------------
          namespace CBS.Infrastructure.AddToRoute;
          public static class DependencyInjection
          {
              public static IServiceCollection AddInfrastructure(this IServiceCollection services, IConfiguration configuration)
              {
                  var connectionString = configuration.GetConnectionString("DefaultConnection");
                  // মাইগ্রেশন রান করা
                  DatabaseMigrator.Migrate(connectionString!);
                  // অন্যান্য সার্ভিস (Dapper, Repositories)
                  services.AddMySqlDataSource(connectionString!);
                  //services.AddScoped<IBranchRepository, BranchRepository>();
                  return services;
              }
          
          }

Create File_02:
---------------
        namespace CBS.Infrastructure.Migration;
        public static class DatabaseMigrator
        {
            public static void Migrate(string connectionString)
            {
                // গুরুত্বপূর্ণ: এখানে Assembly.GetExecutingAssembly() ব্যবহার করায় 
                // এটি Infrastructure লেয়ারের Embedded Scripts গুলো খুঁজে পাবে।
                var upgrader = DeployChanges.To
                    .MySqlDatabase(connectionString)
                    .WithScriptsEmbeddedInAssembly(Assembly.GetExecutingAssembly())
                    .LogToConsole()
                    .Build();
        
                if (upgrader.IsUpgradeRequired())
                {
                    var result = upgrader.PerformUpgrade();
                    if (!result.Successful)
                    {
                        throw new Exception($"Database Migration Failed: {result.Error}");
                    }            
                }
            }
        }


Add below lines to Program.cs
-----------------------------
   using CBS.Infrastructure.AddToRoute;
   // Dependancy Injection from CBS.Infrastructure.AddToRoute for MySQL and Migration
   builder.Services.AddInfrastructure(builder.Configuration);


Run this CMDs and check Wheather Table is created:
--------------------------------------------------
dotnet clean
dotnet build
dotnet run --project src/CBS.Web







### 6. Use of Dapper

using Dapper;           // Dapper অবশ্যই ইমপোর্ট করতে হবে
using MySqlConnector;   // MySqlDataSource এর জন্য

public class BranchService
{
    private readonly MySqlDataSource _dataSource; // DataSource ইনজেক্ট করা হয়েছে
    // কনস্ট্রাক্টরে MySqlDataSource যুক্ত করা হয়েছে
    public BranchService(MySqlDataSource dataSource)
    {
      _dataSource = dataSource;
    }

    public async Task<Result<int>> CreateBranchAsyn(BranchCreateDTO bdto, int UserID)
    {
        // ১. Dapper ব্যবহার করে Branch Code চেক করা
        const string sql = "SELECT COUNT(1) FROM Branches WHERE BranchCode = @BranchCode";        
        //Open database connection as we use "using var" no need to close it
        using var connection = await _dataSource.OpenConnectionAsync();
       // ExecuteScalarAsync দিয়ে খুব দ্রুত রেজাল্ট আনা যায়
        // new { BranchCode = bdto.BranchCode }: এখানে Dapper কে বলা হচ্ছে যে SQL এর @BranchCode প্যারামিটারে আমাদের DTO থেকে আসা আসল ভ্যালুটা বসিয়ে দিতে। এটি একটি Anonymous Object।
        var count = await connection.ExecuteScalarAsync<int>(sql, new { BranchCode = bdto.BranchCode });
}


############################################################### 
                 7. SQL_CRUD 
############################################################### 

using var connection = await _dataSource.OpenConnectionAsync();

var sql = "INSERT INTO Branches (BranchName, BranchCode) VALUES (@BranchName, @BranchCode)";    
    // ড্যাপার অটোমেটিক অবজেক্ট থেকে প্রপার্টিগুলো ম্যাপিং করে নেবে (@ চিহ্নের মাধ্যমে)
    await connection.ExecuteAsync(sql, branch);

var sql = "SELECT id as Id, branch_name as BranchName, branch_code as BranchCode FROM Branches";
    return await connection.QueryAsync<Branch>(sql);

var sql = "SELECT id as Id, branch_name as BranchName, branch_code as BranchCode FROM Branches WHERE Id = @Id";
    return await connection.QueryFirstOrDefaultAsync<Branch>(sql, new { Id = id });

var sql = "UPDATE Branches SET BranchName = @BranchName, BranchCode = @BranchCode WHERE Id = @Id";    
    await connection.ExecuteAsync(sql, branch);

var sql = "DELETE FROM Branches WHERE Id = @Id";
    await connection.ExecuteAsync(sql, new { Id = id });



মেথড,কখন ব্যবহার করবেন?,উদাহরণ
---------------------------------
ExecuteAsync,-----------"ডাটা ইনসার্ট, আপডেট বা ডিলিট করতে।"-------UPDATE Branches SET Name = @Name
ExecuteScalarAsync-------শুধু একটি ভ্যালুর জন্য(Faster)।--------------SELECT COUNT(*), SELECT MAX(Price)

QueryAsync--------------একাধিক রো বা লিস্টের জন্য।------------------SELECT * FROM Branches
QuerySingleAsync--------ঠিক একটি রো আসবে নিশ্চিত হলে।-------------SELECT * FROM Branches WHERE Id = 5






.
