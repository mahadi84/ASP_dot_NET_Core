

এই আর্কিটেকচারে ডাটা ফ্লো সব সময় বাইরে থেকে ভেতরে (Inward) হয়।

🔄 Registration Data Flow (Step-by-Step)

নিচের ছবিটি লক্ষ্য করলে দেখবেন ডাটা কীভাবে বাহিরের লেয়ার (Web) থেকে একদম ভেতরের লেয়ার (Domain) পর্যন্ত প্রবাহিত হয়:

১. Web Layer (The Entry Point)
ইউজার যখন রেজিস্ট্রেশন ফর্মে ডাটা সাবমিট করেন, তখন সেটি OnlineBanking.Web লেয়ারে আসে।
    Controller: AccountController ডাটা গ্রহণ করে।
    ViewModel: ডাটাগুলো RegisterVm অবজেক্টে জমা হয় (এখানেই চেক করা হয় সব ঘর পূরণ হয়েছে কিনা)।

২. Application Layer (The Orchestrator)
কন্ট্রোলার তখন IAuthService কল করে। এই লেয়ারটি মূলত একটি "কন্ট্রাক্ট" বা চুক্তির মতো কাজ করে। এটি বলে দেয় "কী করতে হবে", কিন্তু "কীভাবে করতে হবে" তা এটি জানে না।

৩. Infrastructure Layer (The Doer)
এখানেই আসল কাজ বা লজিকগুলো ঘটে:
    Password Hashing: পাসওয়ার্ডটি BCrypt দিয়ে হ্যাশ করা হয়।
    Account Logic: র্যান্ডমলি একটি ৫ ডিজিটের অ্যাকাউন্ট নম্বর জেনারেট করা হয়।
    Persistence: AppDbContext এর মাধ্যমে ডাটাবেসে সেভ করার নির্দেশ দেওয়া হয়।

৪. Domain Layer (The Core)

সবশেষে ডাটাগুলো Customer এবং Account এনটিটি হিসেবে তৈরি হয়। এটি সিস্টেমের সবচেয়ে সুরক্ষিত এবং ভেতরের লেয়ার।
    এখানে কোনো ডাটাবেস বা এইচটিটিপি (HTTP) জ্ঞান থাকে না।
    এটি শুধু ইউজারের তথ্য (Name, Email, Balance) ধারণ করে।

