
Git কোনো শাস্তির যন্ত্র না—এটা সময়কে শৃঙ্খলায় রাখার যন্ত্র।
একবার ছন্দটা ধরতে পারলে, এগুলো আর error মনে হবে না—warning হয়ে যাবে।


# ✅ CASE–A: লোকাল PC-তে আগে থেকেই .NET প্রজেক্ট আছে (সবচেয়ে common)


git init
    .gitignore ফাইলে এইগুলো রাখো:
    bin/
    obj/
    .vs/
    .user
    .suo
    appsettings.Development.json
git add .
git config --global user.email "mahadi.engr@gmail.com"
git config --global user.name "mahadi**"
git commit -m "Initial commit of .NET project"
git remote add origin https://github.com/mahadi84/MyDotNetWebAppName     
git branch -M main
git push -u origin main




### 🔹 Step-1: GitHub এ নতুন repo তৈরি করো

1. GitHub → New Repository
2. Repo name দাও (যেমন: `Login_and_CRUD`)
3. ❌ README / .gitignore / License কিছুই auto-create করো না
4. Create Repository

⚠️ এটা খুব গুরুত্বপূর্ণ—এতে history conflict হবে না।

### 🔹 Step-2: লোকাল প্রজেক্ট ফোল্ডারে ঢুকো
cd D:/AsPdOtNeT/Login_latest

### 🔹 Step-3: Git initialize করো
git init

### 🔹 Step-4: `.gitignore` ফাইল তৈরি করো (অবশ্যই)

`.gitignore` ফাইলে এইগুলো রাখো:

bin/
obj/
.vs/
.user
.suo
appsettings.Development.json

👉 এতে build file, secret, cache GitHub এ যাবে না।

### 🔹 Step-5: সব file stage করো
git add .

### 🔹 Step-6: প্রথম commit করো
git commit -m "Initial commit of .NET project"

### 🔹 Step-7: GitHub repo-র সাথে connect করো
git remote add origin https://github.com/USERNAME/REPO_NAME.git

উদাহরণ:
git remote add origin https://github.com/mahadi84/Login_and_CRUD.git

### 🔹 Step-8: main branch সেট করো
git branch -M main

### 🔹 Step-9: GitHub এ push করো 🎉
git push -u origin main

✔ Done. Project upload হয়ে গেছে।





# ✅ CASE–B: GitHub এ repo আগে আছে (README সহ)

### 🔹 Step-1: Repo clone করো
git clone https://github.com/mahadi84/Login_and_CRUD.git

### 🔹 Step-2: Project files কপি করো

 তোমার লোকাল .NET project files
 Clone করা folder এর ভেতরে paste করো

### 🔹 Step-3: Commit করো
git add .
git commit -m "Add .NET project"
git push

✔ কোনো conflict হবে না।

# 🧠 Golden Rules (এইগুলো মনে রাখলে Git ভয়ংকর লাগবে না)

1️⃣ GitHub repo থাকলে → clone দিয়ে শুরু করো
2️⃣ Local project থাকলে → GitHub repo empty রাখো
3️⃣ Push করার আগে সবসময়:
git pull

4️⃣ কখনো না বুঝে:
git push -f ❌

# 🎯 এক লাইনের চূড়ান্ত সূত্র

> Empty GitHub repo + local project → git init → commit → push

এই workflow-টা একবার মাথায় বসে গেলে, Git error আর error মনে হবে না—just guidance মনে হবে।

