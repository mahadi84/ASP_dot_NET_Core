
💻 ASP.NET Core MVC এর মূল ধারণা (Core Concepts of ASP.NET Core MVC)



ASP.NET:
What:"এটি একটি ওপেন-সোর্স, ক্রস-প্ল্যাটফর্ম ফ্রেমওয়ার্ক যা মাইক্রোসফট তৈরি করেছে আধুনিক, ক্লাউড-ভিত্তিক ইন্টারনেট-সংযুক্ত অ্যাপ্লিকেশন তৈরির জন্য।",
How: "এটি HTTP রিকোয়েস্ট গ্রহণ করে, MVC প্যাটার্ন (Model-View-Controller) অনুসরণ করে ডেটা প্রসেস করে এবং HTML/JSON ইত্যাদি ফরম্যাটে রেসপন্স তৈরি করে ব্যবহারকারীকে ফেরত পাঠায়।"

CSHTML:
What:"এটি হল রেজর ভিউ ফাইল (Razor View File)। এটি HTML এবং C# কোডের মিশ্রণে তৈরি হয়, যা ভিউ (View)-কে ডায়নামিক ডেটা দেখাতে সাহায্য করে।",
How: "সার্ভারে, ASP.NET কোর রেজর সিনট্যাক্স (@ দিয়ে শুরু) ব্যবহার করে এই ফাইলটিকে সাধারণ HTML-এ রূপান্তরিত করে এবং ব্রাউজারে পাঠায়।"

NAMESAPCE:
What: "এটি C# প্রোগ্রামিং-এ একটি কন্টেইনার বা স্থান, যেখানে ক্লাস (class), ইন্টারফেস (interface) ইত্যাদি গোছানো থাকে। এটি একই নামের ক্লাসকে আলাদা রাখতে সাহায্য করে।",
How: কন্ট্রোলার বা মডেলের ক্লাসগুলি নির্দিষ্ট Namespace-এর মধ্যে সংজ্ঞায়িত করা হয়। _ViewImports.cshtml ফাইলে ভিউ-এর জন্য গ্লোবাল Namespace সেট করা যায়।

USING:
What: "এটি একটি C# কীওয়ার্ড, যা একটি Namespace-কে বর্তমান ফাইলে ব্যবহার করার অনুমতি দেয়। এর ফলে সেই Namespace-এর ক্লাসগুলিকে পুরো নাম (Full Name) ছাড়া ব্যবহার করা যায়।",
How: "আপনি একটি কন্ট্রোলারের শুরুতে using System.Data; লিখলে, আপনি System.Data Namespace-এর সব ক্লাস সরাসরি ব্যবহার করতে পারবেন। _ViewImports.cshtml-এ এটি ভিউগুলির জন্য গ্লোবালি কাজ করে।"

Return type:
What: এটি হল কন্ট্রোলার অ্যাকশন মেথড (Controller Action Method)-এর ফলাফল বা আউটপুট-এর প্রকার। এটি নির্দিষ্ট করে যে মেথডটি কাজ শেষে কী ডেটা ফেরত দেবে।,
How: "MVC তে সাধারণত IActionResult (বা এর থেকে প্রাপ্ত অন্যান্য প্রকার যেমন ViewResult, JsonResult, NotFoundResult) ব্যবহার করা হয়, 
যা MVC ফ্রেমওয়ার্ককে বলে দেয় যে রিকোয়েস্টের প্রতিক্রিয়া হিসাবে কী করতে হবে (যেমন: একটি ভিউ প্রদর্শন, একটি ফাইল ডাউনলোড, বা একটি HTTP স্ট্যাটাস কোড ফেরত দেওয়া)।"



✂️ Segregate: HTML's Header, Footer (HTML-এর Header, Footer ভাগ করা)

01. লেআউট ফাইল (Views/Shared/_Layout.cshtml):
_Layout.cshtml এ <header> এবং <footer> ট্যাগ-এর মধ্যে সাধারণ ডিজাইন রাখা হয়েছে। @RenderBody() ব্যবহার করে প্রতিটি স্বতন্ত্র ভিউ-এর কন্টেন্ট (যেমন Index.cshtml) লোড করা হচ্ছে। 

02. পার্শিয়াল ভিউ ফাইল (Views/Shared/_FooterPartial.cshtml):
Footer-এর কোডকে @await Html.PartialAsync("_FooterPartial") এর মাধ্যমে একটি আলাদা ফাইলে (_FooterPartial.cshtml) রেখে ব্যবহার করা হচ্ছে, যা কোড পুনর্ব্যবহারযোগ্য করে তোলে।


###################################

## ১. 🗺️ Route (রুট)
কী:
রুট হলো একটি প্যাটার্ন (Pattern) যা ASP.NET Core-কে বলে দেয় যে 
একটি নির্দিষ্ট URL-কে অ্যাপ্লিকেশনের কোন Controller-এর কোন Action Method-এর সাথে মেলাতে হবে। 
এটি ঠিক একটি রাস্তার মানচিত্রের মতো।

কীভাবে কাজ করে (How it works):
1.  ব্যবহারকারী ব্রাউজারে একটি URL টাইপ করে (যেমন: `/Products/Details/5`)।
2.  Routing সিস্টেম সেই URL-কে তার সংজ্ঞায়িত রুট টেমপ্লেটের (যেমন: `"{controller}/{action}/{id?}"`) সাথে মেলানোর চেষ্টা করে।
3.  ম্যাচ হলে, এটি URL-এর অংশগুলো থেকে কন্ট্রোলার নাম, অ্যাকশন নাম এবং প্যারামিটার মান (যেমন: `id=5`) বের করে।
4.  এই মানগুলো ব্যবহার করে এটি সঠিক কন্ট্রোলার অ্যাকশন মেথডকে কল করে।


## ২. 🕹️ Controller (কন্ট্রোলার)
কী:
Controller হলো একটি C\# ক্লাস যা ব্যবহারকারীর ইনপুট গ্রহণ, প্রসেস এবং উপযুক্ত প্রতিক্রিয়া (Response) তৈরির জন্য দায়ী। এটি MVC প্যাটার্নের 'C' (Controller) অংশ।

কীভাবে কাজ করে (How it works):
1.  Routing থেকে প্রাপ্ত রিকোয়েস্ট Controller-এর একটি Action Method-এ (যেমন: `Index()` বা `Details(int id)`) পৌঁছায়।
2.  Action Method-টি প্রয়োজন অনুসারে Model-কে ডাকে—হয় ডেটা সংরক্ষণের জন্য, অথবা ডেটা তুলে আনার জন্য।
3.  ডেটা প্রসেস করার পর, Controller নির্ধারণ করে যে ব্যবহারকারীকে কোন View দেখানো হবে এবং সেই View-তে প্রদর্শনের জন্য ডেটা (Model-এর Object) পাঠিয়ে দেয়।
4.  এটি সাধারণত একটি `IActionResult` রিটার্ন করে (যেমন: `return View(data);` বা `return NotFound();`)।

উদাহরণ (C\#):
```csharp
public class ProductsController : Controller // Controller ক্লাস
{
    // Action Method
    public IActionResult Details(int id) 
    {
        // Model থেকে id=5 এর প্রোডাক্ট ডেটা আনা হলো
        var productData = new Product { Id = id, Name = "Laptop", Price = 55000 }; 

        // ডেটা সহ View-কে ফেরত পাঠানো
        return View(productData); // "Details.cshtml" নামে View-কে productData পাঠানো হচ্ছে
    }
}
```

## ৩. 💾 Model (মডেল)
কী:
Model হলো অ্যাপ্লিকেশনের ডেটা এবং ব্যবসায়িক যুক্তি (Business Logic)-এর প্রতিনিধিত্বকারী C\# ক্লাস। এটি MVC প্যাটার্নের 'M' (Model) অংশ।

কীভাবে কাজ করে (How it works):
1.  ডেটা স্ট্রাকচার (Data Structure): এটি ডেটার কাঠামো সংজ্ঞায়িত করে (যেমন: `Product` ক্লাসে `Name`, `Price` প্রোপার্টি)।
2.  ডেটা অ্যাক্সেস (Data Access): এটি ডেটাবেস বা অন্যান্য ডেটা সোর্স থেকে ডেটা যোগ, সম্পাদনা, মুছে ফেলা বা তুলে আনার (CRUD Operations) কাজ করে।
3.  Controller Model থেকে ডেটা নিয়ে আসে এবং এটিকে View-তে প্রদর্শন করার জন্য প্রস্তুত করে।

উদাহরণ (C\#):
```csharp
namespace MyApp.Models
{
    public class Product // Model ক্লাস
    {
        public int Id { get; set; }
        public string Name { get; set; }
        public decimal Price { get; set; }
    }
}
```


## ৪. 🖥️ View (ভিউ)
কী:
View হলো একটি CSHTML (Razor View) ফাইল, যা ব্যবহারকারীর কাছে দেখানো ইউজার ইন্টারফেস (UI) তৈরি করে। এটি MVC প্যাটার্নের 'V' (View) অংশ।

কীভাবে কাজ করে (How it works):
1.  Controller থেকে View-এর কাছে প্রদর্শনের জন্য Model ডেটা আসে।
2.  View এই ডেটা ব্যবহার করে ডায়নামিক HTML তৈরি করে। এতে C\# কোড এবং HTML-এর মিশ্রণ থাকে, যা `@` চিহ্ন দিয়ে চিহ্নিত করা হয়।
3.  View-এর একমাত্র কাজ হলো ডেটা প্রদর্শন করা; এটি কোনো ব্যবসায়িক যুক্তি (Business Logic) পরিচালনা করে না।

উদাহরণ (CSHTML):
উপরে Controller থেকে পাঠানো `productData` গ্রহণ করে `Details.cshtml` ফাইলটি:

```cshtml
@model MyApp.Models.Product @ কোন Model গ্রহণ করা হবে তা নির্ধারণ করে @

<h2>পণ্যের বিবরণ</h2>
<p>আইডি: @Model.Id</p>
<p>নাম: @Model.Name</p>
<p>দাম: @Model.Price টাকা</p>
```


.

