

Explain the code:

// data সহ result (generic version)
public sealed record Result<T>(bool Success, string Message, T? Data){
    public static Result<T> Ok(T data, string msg = "OK")=> new(true, msg, data);  // সফল হলে data সহ result ফেরত দেয়    
    public static Result<T> Fail(string msg) => new(false, msg, default); // ব্যর্থ হলে data = null থাকব
}

## sealed
👉 এই class-টা inherit করা যাবে না 👉 কেউ ভুলভাবে override না করে

## record
👉 C#-এর modern feature 👉 মূলত immutable data carrier
📌 Record সাধারণত DTO / Result / Response-এর জন্য ব্যবহার হয়


Result<T> ❓
Result<T> হলো একটি Generic, Immutable, Data-carrier class (record), যা কোনো operation-এর outcome (success/failure + data) বহন করে।


## <T> (Generic)
Result<T>: 👉 T মানে: যেকোনো data type - string, int, Customer, List<User>—সব হতে পারে উদাহরণ: Result<string>/Result<Customer>/Result<int>


## (bool Success, string Message, T? Data)
T? মানে কী? T? বোঝায় যে Result-এর Data property nullable, অর্থাৎ operation ব্যর্থ হলে এটি null হতে পারে।

কেন T? দরকার?
Result<T> দুই রকম অবস্থা বোঝায়:  ✅ Success হলে → Data থাকবে ❌ Fail হলে → Data থাকবে না (null)

উদাহরণ দিয়ে বুঝি
Success case: Result<string>.Ok("User created");
ভেতরে হবে: Success = true Data = "User created"

Fail case:  Result<string>.Fail("Email already exists");

ভেতরে হবে: Success = false Data = null

👉 এই null allow করার জন্যই T?
🔹 T? না দিলে কী হতো? public sealed record Result<T>(bool Success, string Message, T Data)
এখন Fail করলে: new Result<T>(false, msg, null); // ❌ error
কারণ: Compiler বলে: T null নাও হতে পারে





######
1. public static Result<T> Fail(string msg)=> new(false, msg, default);
Vs
2. public static Result<T> Fail(string msg){return new Result<T>(false, msg, default);}

- Both Statement is Same
1. ❓ এখানে new আসলে কী?
এটা হলো Target-typed new expression (C# 9+ feature)।
👉 এখানে new লিখে class নাম (Result<T>) লেখা হয়নি, কারণ compiler আগেই জানে কোন type return করতে হবে।






